name: vqa-env
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10  
  - pip
  - pytorch=2.2.2 
  - pytorch-cuda=12.1
  - torchvision       # Indispensable para procesar imágenes (tensores)
  - numpy>=1.25.2
  - pandas
  - matplotlib
  - seaborn
  - scikit-learn
  - tqdm
  - rich              # Para logs bonitos en terminal
  - python-dotenv     # Para manejar variables de entorno
  - pyyaml            # Para leer tus configs/yaml
  - pip:
      - transformers>=4.41.0  
      - accelerate>=0.30.0
      - datasets
      - peft              # Conservado
      - bitsandbytes      # Conservado (para 4-bit loading)
      - pillow            # Manipulación básica de imágenes
      - opencv-python     # A veces necesario para pre-procesamiento avanzado
      - evaluate
      - sacrebleu         # Para BLEU
      - rouge_score       # Para ROUGE-L
      - bert_score        # Opcional, pero potente
      - nltk              # Conservado
      - wandb             # Conservado